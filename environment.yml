name: pv-code-examples

channels: 
  - conda-forge

dependencies:
  # R dependencies
  - r-base=4.4
  - r-essentials

  # Python dependencies
  - python=3.13
  - pip
  - pip:
    - jupyter
    - ipykernel
    - pandas
    - duckdb